# platforms-mac

## Latency
verdict: cannot the output handle is a raw AudioUnit pointer used with CoreAudio calls in C, which Haxe code cannot hold or make. FmodSettings.dspBufferSize is the latency setting the library exposes
