# welcome-whats-new-201

## Thread attributes
verdict: library thread attributes are set before System_Create, which the library performs itself, so FMOD keeps its default priority, affinity, and stack size for every worker thread on each native target and the web build has no threads to configure

## Thread attributes#2
verdict: library thread attributes are set before System_Create, which the library performs itself, so FMOD keeps its default priority, affinity, and stack size for every worker thread on each native target and the web build has no threads to configure
