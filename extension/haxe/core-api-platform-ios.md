# core-api-platform-ios

## FMOD_AUDIOQUEUE_CODECPOLICY
verdict: cannot iOS is not a haxefmod target, the library ships Windows, Linux, and macOS (C++ and HashLink) and HTML5 builds only, and this enum exists only in the iOS headers
