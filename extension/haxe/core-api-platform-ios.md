# core-api-platform-ios

## FMOD_AUDIOQUEUE_CODECPOLICY
verdict: cannot iOS is not a haxefmod target. The library ships Windows, Linux, and macOS (C++ and HashLink) and HTML5 builds only. This enum exists only in the iOS headers
