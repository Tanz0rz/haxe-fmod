# fsbank-api

## 20.10 FSBANK_MEMORY_ALLOC_CALLBACK
verdict: cannot an allocator hook for the FSBank offline encoder, which is a separate tool library outside the FMOD runtime and is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.11 FSBANK_MEMORY_FREE_CALLBACK
verdict: cannot an allocator hook for the FSBank offline encoder, which is a separate tool library outside the FMOD runtime and is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.12 FSBANK_MEMORY_REALLOC_CALLBACK
verdict: cannot an allocator hook for the FSBank offline encoder, which is a separate tool library outside the FMOD runtime and is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.13 FSBANK_INITFLAGS
verdict: cannot init flags of the FSBank offline encoder, a separate tool library outside the FMOD runtime that is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.14 FSBANK_BUILDFLAGS
verdict: cannot build flags of the FSBank offline encoder, a separate tool library outside the FMOD runtime that is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.15 FSBANK_FORMAT
verdict: cannot the encoding formats of the FSBank offline encoder, a separate tool library outside the FMOD runtime that is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.16 FSBANK_FSBVERSION
verdict: cannot the output version of the FSBank offline encoder, a separate tool library outside the FMOD runtime that is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.17 FSBANK_PROGRESSITEM
verdict: cannot a build progress record of the FSBank offline encoder, a separate tool library outside the FMOD runtime that is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.18 FSBANK_RESULT
verdict: cannot the result codes of the FSBank offline encoder, a separate tool library outside the FMOD runtime that is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.19 FSBANK_STATE
verdict: cannot the build states of the FSBank offline encoder, a separate tool library outside the FMOD runtime that is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.20 FSBANK_STATEDATA_FAILED
verdict: cannot a build failure record of the FSBank offline encoder, a separate tool library outside the FMOD runtime that is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.21 FSBANK_STATEDATA_WARNING
verdict: cannot a build warning record of the FSBank offline encoder, a separate tool library outside the FMOD runtime that is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.

## 20.22 FSBANK_SUBSOUND
verdict: cannot the input description for the FSBank offline encoder, a separate tool library outside the FMOD runtime that is not bound
Build banks in FMOD Studio and load them with StudioSystem.loadBankFile. An existing FSB file loads with Sound.create.
